import { introspectionQuery, buildClientSchema } from 'graphql';
export function getClientSchema(connector) {
  return connector({
    query: introspectionQuery
  }, {
    applyMigrations: false
  }).then(res => buildClientSchema(res.data));
}
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.handleHttpErrors = handleHttpErrors;

function handleHttpErrors(e, activity) {
  if (e.statusCode === 401 || e.statusCode === 403) {
    throw new Error('Insufficient Permissions to develop with TakeShape CLI. Your account must have the "Admin" role or better.');
  } else if (e.statusCode === 500) {
    throw new Error(`A Server error occurred while ${activity}.`);
  }

  throw e;
}